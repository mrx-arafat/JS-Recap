const nums = [112, 32, 33, 3, 7];

function myFunction(value) {
  return value * 2;
}

const newNums = nums.map(myFunction);

console.log(newNums);

//what is the difference between forEach and map

// think once map amare ekta new array return korteche

// forEach amare same array tei change kore dibe
